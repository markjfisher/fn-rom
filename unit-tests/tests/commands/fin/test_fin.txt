///////////////////////////////
marker $$ test *FIN foo.ssd $$
///////////////////////////////

memory load rom #0x8000 "${WS_ROOT}/build/fujinet.rom"
symbols load "${WS_ROOT}/build/fujinet.rom.lbl"
memory load atari "${UNIT_TEST_DIR}/harness/harness.xex"
symbols load "${UNIT_TEST_DIR}/harness/build/harness.lbl"

memory protect ro #0xFF00 0x0100

registers flush

// initialise the ROM
run $init_harness until CP = $end_init_harness

; emulate a command from MOS
; Y = offset into cmd line buffer = 3, always length of command
; A/X not used

registers set cycle_count = 0
registers set Y = 3

// run $cmd_fs_fin while cycle_count < 50000
