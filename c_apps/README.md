# c_apps

cc65 C applications for the BBC using fork of cc65 that supports that target.

## build

There is a script that will build the SSD containing any c applications.

```shell
export CC65_SRC=/path/to/your/cc65-fork
./c_apps/create_ctest_ssd.sh
```
